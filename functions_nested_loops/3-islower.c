#include "main.h"
/**
 * main - xd
 * Return: 0, 1
 */
int _islower(int c)
{
	char i;
	int resultado;

	for (i = 'a'; i <= 'z'; ++i)
	{
		if (c == i)
			return (resultado);
		else
			return (resultado);
	}

}
